package student.y2_collect;

/*
    Because it's a common task to collect all results from a stream into a list, there are some predefined collectors:
        - Collectors.toList()
        - Collectors.groupingBy()
        - Collectors. ...

    // TODO use Collectors.toList() to collect all email addresses. Print the result.
    // TODO use Collectors.groupingBy() to gets two lists sorted by the gender.

 */
public class Y22Collectors {

    public static void main(String[] args) {
        collectToList();
        groupByGender();
    }

    private static void collectToList() {
    }

    private static void groupByGender() {
    }


}
